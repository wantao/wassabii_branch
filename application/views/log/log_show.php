<?php
	echo "<meta charset=\"utf-8\">";
	echo "<div align=\"center\"> <a href="."/game_log/"."   target=\"_blank\" >".LG_EVERY_AREA_LOG."</a><br> </div>";
	/*while (list($key, $val) = each($folder_list))
  	{
  		print_r($key);
  		echo "<a href=/game_log/".$key."</a><br>";
	  	while(list($subkey, $subvalue) = each($val)){
	  		foreach($subvalue as $filename){
	  			
	  			echo "<a href=/game_log/".$key."/".$subkey."/".$filename.">".$filename."</a><br>";
	  		}
	  	}
    }*/
?>

